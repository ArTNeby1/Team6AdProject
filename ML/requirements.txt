

# --- Web 框架 ---
#flask>=3.0,<4.0
fastapi>=0.110,<1.0
uvicorn[standard]>=0.29,<1.0

# --- 数据库 ---
# sqlalchemy>=2.0,<3.0
# psycopg2-binary>=2.9

# --- 数据处理 / ML ---
pandas>=2.0,<3.0
scikit-learn>=1.4,<2.0  # content_recommender.py 的 TF-IDF + 余弦相似度要用

# --- 其他业务依赖 ---
requests>=2.31,<3.0  # local_llm_client.py 调用本地 Ollama 的 HTTP API 要用
boto3>=1.34,<2.0  # bedrock_client.py 调用 AWS Bedrock runtime 要用

{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "TripExtraction",
  "description": "Structured trip data extracted from travel text. Field design notes: field_notes.md",
  "type": "object",
  "required": ["destination", "places"],
  "additionalProperties": false,
  "properties": {
    "destination": {
      "type": "string",
      "description": "The city or region of the whole trip, e.g. 'Singapore'"
    },
    "dates": {
      "type": "array",
      "default": [],
      "description": "Calendar dates explicitly stated in the text, format YYYY-MM-DD. If the text only says Day 1 / Day 2 with no real dates, return an empty array. Never invent a year.",
      "items": {
        "type": "string",
        "format": "date"
      }
    },
    "duration_days": {
      "type": ["integer", "null"],
      "default": null,
      "minimum": 1,
      "maximum": 30,
      "description": "Total number of days the whole trip lasts, if the text states it (e.g. 'a 3-day trip', or the text is organised as Day 1 / Day 2 / Day 3). Never guess or infer this from the number of places: return null when the text does not say how long the trip is."
    },
    "places": {
      "type": "array",
      "minItems": 1,
      "description": "Every place mentioned in the text",
      "items": {
        "type": "object",
        "required": ["name", "type"],
        "additionalProperties": false,
        "properties": {
          "name": {
            "type": "string",
            "maxLength": 255,
            "description": "Place name only, never a whole sentence. Max 255 chars to match the backend draft_place.name column."
          },
          "type": {
            "type": "string",
            "enum": ["attraction", "restaurant", "hotel", "market", "other"],
            "description": "Place category. Must be exactly one of the five enum values."
          },
          "coords": {
            "type": ["object", "null"],
            "default": null,
            "description": "Latitude and longitude. Travel text almost never contains coordinates. Never invent them: always return null here. Real coordinates are filled in later by a geocoding step.",
            "required": ["lat", "lng"],
            "additionalProperties": false,
            "properties": {
              "lat": { "type": "number" },
              "lng": { "type": "number" }
            }
          },
          "activities": {
            "type": "array",
            "default": [],
            "description": "What the traveller does at this place",
            "items": { "type": "string", "maxLength": 255 }
          }
        }
      }
    }
  }
}
